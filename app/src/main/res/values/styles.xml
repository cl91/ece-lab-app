<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/ECEActionBar</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppThemeNoActionBar" parent="android:Theme.Holo.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="ECEActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/ECEPurple</item>
        <item name="android:titleTextStyle">@style/ECEActionBarText</item>
        <item name="android:icon">@drawable/ic_alt</item>
    </style>

    <style name="ECEActionBarText" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/White</item>
    </style>

    <style name="MarkingDialogGG" parent="@android:style/Theme.Dialog">

    </style>


</resources>
